# Submission category and provisional scorecard

## Category

`PROJECT`

This is an end-to-end product: a real Intelligent Contract owns the consensus-critical assessment; a static frontend reads it and submits all four public workflow writes; the integration is configured for Studionet only. It is not submitted as a standalone reusable contract.

## Trust relation

Journalists, researchers, and citizens cannot safely rely on an advertiser, submitter, or ordinary application server to preserve an exact ad artifact or select neutral sponsor evidence. A submitter can change the artifact URL, disclaimer, committee claim, candidate relation, or observation window; an application server could select favorable records or rewrite the result.

The contract freezes those inputs, independently fetches the exact artifact plus official OpenFEC committee and Schedule E evidence, and stores only a validator-agreed consequential projection. Unavailable, malformed, conflicting, weak-provenance, or model-inconsistent evidence cannot become a positive compatibility result. The on-chain consequence is an immutable assessment revision linked to the frozen trace.

## Post-deployment evidence-based scorecard

GENLAYER SUBMISSION CATEGORY AND SCORECARD

Category: PROJECT  
Validity gate: PASS for the approved source, release deployment, live contract matrix, and isolated upgrade rehearsal; anonymous `POST_DEPLOY_TEST` review remains pending

GenLayer fit: 4/5  
Evidence: `contracts/campaign_ad_sponsor_trace.py` makes independent web retrieval and semantic matching consensus-critical; the consequential projection is validator-rederived and stored on-chain.  
Weakness/blocker: the independent `POST_DEPLOY_TEST` verdict remains pending.

Contract quality: 4/5  
Evidence: frozen inputs, derived provenance, exact artifact/FEC digests, typed safe verdicts, append-only reassessment, result-consistency guards, custom validator projection, upgradable Root Slot policy, lint and semantic validation.  
Weakness/blocker: rate-limited external evidence correctly produced a safe `UNRESOLVED` revision; the exact evidence package still requires independent checkpoint review.

Engineering: 3/5  
Evidence: reproducible Python/Node tests, production build, pinned dependency, explicit transaction reconciliation, deployment recovery document, and no new installed dependency.  
Weakness/blocker: the current post-deployment evidence package still needs exact-revision independent review.

Frontend / UX: 3/5  
Evidence: real `genlayer-js` calls, explicit wallet provider chooser, lossless integer boundary, FINALIZED/SUCCESS/readback lifecycle, and responsive browser QA.  
Weakness/blocker: browser UX against the release address still needs the final hosted-environment checkpoint after repository approval.

Overall evidence-based assessment: the release deployment, network behavior, rollback controls, and isolated upgrade recovery path are verified. The user's Task-local Studio override is disclosed in the evidence package. Independent checkpoint review remains the release gate before GitHub/Vercel work.
Submission recommendation: NOT READY for submission; next gate is anonymous `POST_DEPLOY_TEST` review.
